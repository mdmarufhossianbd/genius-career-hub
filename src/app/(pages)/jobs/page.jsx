import { Button } from '@/components/ui/button';

const Jobs = () => {
    return (
        <div>
            This is jobs page <Button>Click Me</Button>
        </div>
    );
};

export default Jobs;